import type { MetadataRoute } from 'next';
import { reviews,categories } from '@/data/site';
export default function sitemap():MetadataRoute.Sitemap{const base='https://hiperfoco.eu';return [{url:base,lastModified:new Date(),changeFrequency:'weekly',priority:1},{url:`${base}/comparativas/`,lastModified:new Date(),changeFrequency:'weekly',priority:.8},{url:`${base}/guias/`,lastModified:new Date(),changeFrequency:'weekly',priority:.8},{url:`${base}/metodologia/`,lastModified:new Date(),changeFrequency:'monthly',priority:.6},{url:`${base}/como-nos-financiamos/`,lastModified:new Date(),changeFrequency:'monthly',priority:.5},...categories.map(c=>({url:`${base}/categoria/${c.slug}/`,lastModified:new Date(),changeFrequency:'weekly' as const,priority:.8})),...reviews.map(r=>({url:`${base}/reviews/${r.slug}/`,lastModified:new Date(),changeFrequency:'weekly' as const,priority:.9}))]}
